% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utility.R
\name{cov_grid_2d}
\alias{cov_grid_2d}
\title{Grid of covariate values over two covariates}
\usage{
cov_grid_2d(
  var,
  var2,
  data = NULL,
  obj = NULL,
  covs = NULL,
  formulas,
  n_grid = 40
)
}
\arguments{
\item{var}{Name of the first covariate, on the x axis}

\item{var2}{Name of the second covariate, on the y axis}

\item{data}{Data frame of covariate values}

\item{obj}{HMM model object, used to find the covariates if \code{data} is
not given}

\item{covs}{Optional named list of values for the other covariates}

\item{formulas}{List of model formulas}

\item{n_grid}{Number of points along each axis, so the grid has
\code{n_grid^2} rows}
}
\value{
Data frame with \code{n_grid^2} rows and one column for each
covariate
}
\description{
The two-dimensional counterpart of \code{\link{cov_grid}}, used by
\code{HMM$plot_2d()}. Both \code{var} and \code{var2} are varied over a
regular \code{n_grid} by \code{n_grid} lattice spanning their ranges in the
data, and every other covariate is held at one value, as in
\code{\link{cov_grid}}: the one given in \code{covs}, otherwise the mean of
a numeric covariate or the first level of a factor.
}
\details{
The lattice is regular because that is what \code{ggplot2::geom_raster()}
needs, and \code{var} varies fastest, so that the result can be reshaped
into a matrix column by column.
}
